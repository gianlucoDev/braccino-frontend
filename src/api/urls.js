// http://localhost:8000
const BASE_URL = `http://${window.location.hostname}:8000`;

// ws://localhost:8000/ws
const BASE_URL_WS = `ws://${window.location.hostname}:8000/ws`;

export { BASE_URL, BASE_URL_WS };
